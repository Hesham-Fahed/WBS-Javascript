<!DOCTYPE html> 
<html lang="en">
<head>
    <title>{% block title %}Rinder!{% endblock %}</title>
    {{ include('parts/head.twig') }}
</head>

<body>

    {{ include('parts/nav.twig') }}

        {% if messages %}
            {{ include('parts/messages.twig') }}
        {% endif %}

        {% block content %}
            Content Block
        {% endblock %}

    {{ include('parts/foot.twig') }}

</body>
</html>
