<nav class="ui four item menu">
    <a class="item" href="{{ basePath }}">Test</a>
    {% if logged_in %}
        <a class="item" href="{{ basePath }}/logout">Logout</a>
    {% else %}
        <a class="item" href="{{ basePath }}/login">Login</a>
    {% endif %}
</nav>
